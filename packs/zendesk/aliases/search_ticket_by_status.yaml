---
name: "search_ticket_by_status"
action_ref: "zendesk.search_ticket"
description: "Search a Zendesk ticket by ticket status"
formats:
    - "search_by_status zendesk ticket {{ status }} "
    - "zendesk search_by_status ticket {{ status }} "
ack:
    format: "Connecting Zendesk server for ticket search"
result:
    format: "Zendesk ticket search details {~} {{ execution.result.result }}"
